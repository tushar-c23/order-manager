<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="X-UA-Compatible" content="ie=edge">
<title></title>
<link rel="stylesheet" href="style.css">
</head>
<body>
    <% for(let order of orders) { %>
        <div>
            <% if(order.Status == 2) { %>
                    Name:<%= order.CustomerName %>
                    Address:<%= order.CustomerAddr %>
                    <form action="/worker" method="POST">
                        <ul>
                            <% let productsChecked = [] %>
                            <% for(let item of order.Items) {%>
                                <% if(!item.isComplete) {%>
                                    <li><%=item.ProductName%>-<%= item.Qty %> <input type="checkbox" id= <%=item.ProductName%> name = <%=item.ProductName%>></li>
                                    <% productsChecked.push(item._id) %>
                                <% } %>
                            <% } %>
                            <input type="hidden" name="orderId" value=<%= order._id %>>
                            <input type="hidden" name="productsId" value=<%= productsChecked %>>
                        </ul>
                        <button>
                            Submit
                        </button>
                    </form>
                <!--A form with choice to assign to worker dropdown choice-->
            <% } %>            
        </div>
    <% } %>
<!-- <script src="app.js"></script> -->
</body>
</html>